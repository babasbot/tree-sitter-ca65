===================
constant assignment
===================

zeropage = $00

---

(asm
  (macro
    (constant_assignment
      (symbol)
      (exp
        (num_32)))))

===================
label assignment
===================

loop := $0000

---

(asm
  (macro
    (symbol_assignment
      (symbol)
      (num_8
        (hex_num_8)))))

===================
standard labels
===================

loop:
  NOP
  JMP loop

---

(asm
  (macro
    (symbol_assignment
      (symbol)))
  (inst
    (impl_opc
      (nop_opc)))
  (inst
    (abs_opc
      (jmp_opc)
      (symbol))))

===================
cheap labels
===================

@loop:
  NOP
  JMP @loop

---

(asm
  (macro
    (symbol_assignment
      (symbol)))
  (inst
    (impl_opc
      (nop_opc)))
  (inst
    (abs_opc
      (jmp_opc)
      (symbol))))

